---
- name: Restart caddy
  when: _caddy_restart_needed
  become: true
  become_user: "{{ caddy_user_name }}"
  ansible.builtin.systemd_service:
    service: "{{ caddy_service_unit }}"
    state: restarted
    scope: user
    daemon_reload: true
  changed_when: true
